<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pizzapp.v2">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".misc.orderplz"></activity>
        <activity android:name=".curActivityClasses.CurDeliveriesActivity" />
        <activity android:name=".adminActivityClasses.UpdateCurActivity" />
        <activity android:name=".curActivityClasses.CurProfActivity" />
        <activity android:name=".adminActivityClasses.ManageCourierActivity" />
        <activity android:name=".curActivityClasses.CurMenuActivity" />
        <activity android:name=".adminActivityClasses.RegCurActivity" />
        <activity android:name=".adminActivityClasses.AdminMenuActivity" />
        <activity android:name=".misc.LoginActivity" />
        <activity android:name=".misc.fakeLoad">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>