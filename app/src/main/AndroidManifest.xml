<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pizzapp.v2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_round"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".CurProfActivity"></activity>
        <activity android:name=".ManageCourierActivity" />
        <activity android:name=".CurMenuActivity" />
        <activity android:name=".RegCurActivity" />
        <activity android:name=".AdminMenuActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".fakeLoad">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>